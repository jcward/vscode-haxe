// Generated by Haxe
(function ($hx_exports) { "use strict";
var $estr = function() { return js_Boot.__string_rec(this,''); };
var EReg = function(r,opt) {
	opt = opt.split("u").join("");
	this.r = new RegExp(r,opt);
};
EReg.__name__ = true;
EReg.prototype = {
	match: function(s) {
		if(this.r.global) this.r.lastIndex = 0;
		this.r.m = this.r.exec(s);
		this.r.s = s;
		return this.r.m != null;
	}
};
var DisplayMode = { __ename__ : true, __constructs__ : ["Default","Position","Usage","Type","TopLevel","Resolve"] };
DisplayMode.Default = ["Default",0];
DisplayMode.Default.toString = $estr;
DisplayMode.Default.__enum__ = DisplayMode;
DisplayMode.Position = ["Position",1];
DisplayMode.Position.toString = $estr;
DisplayMode.Position.__enum__ = DisplayMode;
DisplayMode.Usage = ["Usage",2];
DisplayMode.Usage.toString = $estr;
DisplayMode.Usage.__enum__ = DisplayMode;
DisplayMode.Type = ["Type",3];
DisplayMode.Type.toString = $estr;
DisplayMode.Type.__enum__ = DisplayMode;
DisplayMode.TopLevel = ["TopLevel",4];
DisplayMode.TopLevel.toString = $estr;
DisplayMode.TopLevel.__enum__ = DisplayMode;
DisplayMode.Resolve = function(v) { var $x = ["Resolve",5,v]; $x.__enum__ = DisplayMode; $x.toString = $estr; return $x; };
var HaxeClient = function(host,port) {
	this.host = host;
	this.port = port;
	this.reset();
};
HaxeClient.__name__ = true;
HaxeClient.isOptionExists = function(optionName,data) {
	var re = new EReg("unknown option '" + optionName + "'","");
	return !re.match(data);
};
HaxeClient.prototype = {
	reset: function() {
		this.cmds = [];
		this.patcher = null;
	}
	,cwd: function(dir) {
		this.cmds.push("--cwd " + dir + "\n");
		return this;
	}
	,version: function() {
		this.cmds.push("-version\n");
		return this;
	}
	,display: function(fileName,pos,mode) {
		var dm;
		switch(mode[1]) {
		case 0:
			dm = "";
			break;
		case 1:
			dm = "@position";
			break;
		case 2:
			dm = "@usage";
			break;
		case 3:
			dm = "@position";
			break;
		case 4:
			dm = "@toplevel";
			break;
		case 5:
			var v = mode[2];
			dm = "@resolve@" + v;
			break;
		}
		this.cmds.push("--display " + fileName + "@" + pos + dm + "\n");
		return this;
	}
	,custom: function(data) {
		this.cmds.push(data + "\n");
		return this;
	}
	,beginPatch: function(fileName) {
		this.endPatch();
		this.patcher = new Patcher(fileName);
		return this.patcher;
	}
	,endPatch: function() {
		if(this.patcher != null) {
			this.cmds.push(this.patcher.get_cmd());
			this.patcher = null;
		}
	}
	,sendAll: function(onData,onError,onClose) {
		var _g = this;
		this.endPatch();
		var s = new Socket();
		s.connect(this.host,this.port,$bind(this,this._onConnect),onData,function(s1,err) {
			_g.reset();
			if(onError != null) onError(s1,err);
		},onClose);
		return s;
	}
	,_onConnect: function(s) {
		var _g = 0;
		var _g1 = this.cmds;
		while(_g < _g1.length) {
			var cmd = _g1[_g];
			++_g;
			s.write(cmd);
		}
		s.write("\x00");
		this.reset();
	}
	,isPatchAvailable: function(onData) {
		var _g = this;
		var oldCmds = this.cmds;
		var oldPatcher = this.patcher;
		this.reset();
		this.beginPatch("~.hx").remove();
		this.version();
		this.sendAll(function(s,data) {
			_g.cmds = oldCmds;
			_g.patcher = oldPatcher;
			var b = HaxeClient.isOptionExists("--patch",data);
			onData({ isServerAvailable : true, isOptionAvailable : b});
		},function(s1,err) {
			_g.cmds = oldCmds;
			_g.patcher = oldPatcher;
			onData({ isServerAvailable : false, isOptionAvailable : false});
		});
	}
};
var HxOverrides = function() { };
HxOverrides.__name__ = true;
HxOverrides.substr = function(s,pos,len) {
	if(len == null) len = s.length; else if(len < 0) {
		if(pos == 0) len = s.length + len; else return "";
	}
	return s.substr(pos,len);
};
var Main = function() { };
Main.__name__ = true;
Main.main = $hx_exports["activate"] = function(context) {
	Main.test_register_command(context);
	var server = new CompletionServer(Vscode.workspace.rootPath);
	var handler = new CompletionHandler(server,context);
	context.subscriptions.push({ dispose : function() {
		Vscode.window.showInformationMessage("Got dispose!");
	}});
};
Main.test_register_command = function(context) {
	var disposable = Vscode.commands.registerCommand("haxe.hello",function() {
		Vscode.window.showInformationMessage("Hello from haxe!");
	});
	context.subscriptions.push(disposable);
};
Main.test_register_hover = function(context) {
	var disposable = Vscode.languages.registerHoverProvider("haxe",{ provideHover : function(document,position,cancelToken) {
		return new Vscode.Hover("I am a hover! pos: " + JSON.stringify(position));
	}});
	context.subscriptions.push(disposable);
};
Main.test_register_hover_thenable = function(context) {
	var disposable = Vscode.languages.registerHoverProvider("haxe",{ provideHover : function(document,position,cancelToken) {
		var s = JSON.stringify(position);
		return new Promise(function(resolve) {
			var h = new Vscode.Hover("I am a thenable hover! pos: " + s);
			resolve(h);
		});
	}});
	context.subscriptions.push(disposable);
};
var CompletionItemProvider = function() { };
CompletionItemProvider.__name__ = true;
var CompletionHandler = function(server,context) {
	this.server = server;
	var disposable = Vscode.languages.registerCompletionItemProvider("haxe",this,".");
	context.subscriptions.push(disposable);
	var removePatch = function(document) {
		if(server.isPatchAvailable) {
			var path = document.uri.fsPath;
			var client = server.client;
			client.beginPatch(path).remove();
			client.sendAll(null,null);
		}
	};
	context.subscriptions.push(Vscode.workspace.onDidOpenTextDocument(removePatch));
	context.subscriptions.push(Vscode.workspace.onDidSaveTextDocument(removePatch));
	context.subscriptions.push(Vscode.workspace.onDidCloseTextDocument(removePatch));
	var changePatch = function(event) {
		if(server.isPatchAvailable) {
			var changes = event.contentChanges;
			if(changes.length > 0) {
				var document1 = event.document;
				var docText = document1.getText();
				var client1 = server.client;
				var patcher = client1.beginPatch(document1.uri.fsPath);
				var _g = 0;
				while(_g < changes.length) {
					var change = changes[_g];
					++_g;
					var rl = change.rangeLength;
					var range = change.range;
					var rs = document1.offsetAt(range.start);
					if(change.rangeLength > 0) patcher["delete"](rs,rl,"c");
					var text = change.text;
					if(text != "") patcher.insert(rs,text,"c");
				}
				client1.sendAll(null,null);
			}
		}
	};
	context.subscriptions.push(Vscode.workspace.onDidChangeTextDocument(changePatch));
};
CompletionHandler.__name__ = true;
CompletionHandler.__interfaces__ = [CompletionItemProvider];
CompletionHandler.prototype = {
	provideCompletionItems: function(document,position,cancelToken) {
		var _g = this;
		var line = document.lineAt(position);
		var dot_offset = 0;
		var subline = HxOverrides.substr(line.text,0,position.character);
		if(subline.indexOf(".") >= 0) dot_offset = subline.lastIndexOf(".") - position.character + 1;
		var byte_pos = document.offsetAt(position) + dot_offset;
		byte_pos = Tool.byteLength((function($this) {
			var $r;
			var _this = document.getText();
			$r = HxOverrides.substr(_this,0,byte_pos);
			return $r;
		}(this)));
		var path = document.uri.fsPath;
		return new Promise(function(resolve) {
			var make_request = function() {
				_g.server.request(path,byte_pos,function(items) {
					resolve(items);
				});
			};
			var isDirty = document.isDirty;
			var client = _g.server.client;
			var doRequest = function() {
				if(_g.server.isPatchAvailable) make_request(); else if(isDirty && _g.server.isServerAvailable) document.save().then(make_request); else make_request();
			};
			if(!_g.server.isServerAvailable) {
				var hs = _g.server.make_client();
				var patcher = hs.beginPatch(path);
				if(isDirty) patcher["delete"](0,-1).insert(0,document.getText()); else patcher.remove();
				_g.server.isPatchAvailable = false;
				hs.version().sendAll(function(s,data) {
					var b = HaxeClient.isOptionExists("--patch",data);
					_g.server.isPatchAvailable = b;
				},function(s1,err) {
					doRequest();
				},function(s2) {
					_g.server.isServerAvailable = !s2.hasError;
					doRequest();
				});
			} else doRequest();
		});
	}
	,resolveCompletionItem: function(item,cancelToken) {
		return item;
	}
};
var ChildProcess = require("child_process");
var CompletionServer = function(proj_dir) {
	var _g = this;
	this.proj_dir = proj_dir;
	this.host = "127.0.0.1";
	this.port = 6000;
	this.isServerAvailable = false;
	this.isPatchAvailable = false;
	this.client = new HaxeClient(this.host,this.port);
	this.client.isPatchAvailable(function(data) {
		_g.isPatchAvailable = data.isOptionAvailable;
		_g.isServerAvailable = data.isServerAvailable;
	});
	var exec = ChildProcess.exec;
	var restart = null;
	restart = function() {
		Vscode.window.showInformationMessage("Starting haxe completion server...");
		exec("haxe --wait " + _g.port,restart);
	};
};
CompletionServer.__name__ = true;
CompletionServer.prototype = {
	make_client: function() {
		return new HaxeClient(this.host,this.port);
	}
	,parse_items: function(data) {
		var rtn = [];
		var data_str = data.stderr;
		
                  // Hack hack hack
                  var items = data_str.split("<i n=");
                  for (var i=0; i<items.length; i++) {
                    var item = items[i];
                    if (item.indexOf("\"")==0) {
                      var name = item.match(/"(.*?)"/)[1];
                      var type = item.match(/<t>(.*?)<\/t>/)[1];
                      type = type.replace(/&gt;/g, ">");
                      type = type.replace(/&lt;/g, "<");
                      //Vscode.window.showInformationMessage(name+" : "+type);
                      var ci = new Vscode.CompletionItem(name);
                      ci.detail = type;
                      if (type.indexOf("->")>=0) {
                        ci.kind = Vscode.CompletionItemKind.Method;
                      } else {
                        ci.kind = Vscode.CompletionItemKind.Property;
                      }
                      rtn.push(ci);
                    }
                  }
     ;
		return rtn;
	}
	,request: function(file,byte_pos,callback) {
		var _g = this;
		var hxml_file = "build.hxml";
		var dir = this.proj_dir;
		var decode;
		
    decode = function decode(data) {
      var stdout = "";
      var stderr = "";
      var hasError = false;
      var i = 0;
      var data = data.split("\n");
      while(i < data.length) {
      	var line = data[i];
      	++i;
      	switch(line.charCodeAt(0)) {
      	case 1:
      		stdout += line.substr(1).split("\x01").join("\n");
      		break;
      	case 2:
      		hasError = true;
      		break;
      	default:
      		stderr += line + "\n";
      	}
      }

      //Vscode.window.showInformationMessage("decoded: "+(stdout.length+stderr.length)+" bytes");

      return { stdout:stdout, stderr:stderr, hasError:hasError }
    }
;
		this.client.cwd(dir);
		this.client.custom(" " + hxml_file);
		this.client.display(file,byte_pos,DisplayMode.Default);
		this.client.sendAll(function(s,data) {
		},function(s1,err) {
			_g.isServerAvailable = false;
			Vscode.window.showErrorMessage(err.message);
			callback([]);
		},function(s2) {
			_g.isServerAvailable = !s2.hasError;
			var data1 = s2.datas.join("");
			callback(_g.parse_items(decode(data1)));
		});
	}
};
Math.__name__ = true;
var Patcher = function(fileName) {
	this.fileName = fileName;
	this.actions = [];
};
Patcher.__name__ = true;
Patcher.prototype = {
	reset: function() {
		this.actions = [];
		return this;
	}
	,remove: function() {
		this.actions.push("x\x01");
		return this;
	}
	,'delete': function(pos,len,unit) {
		if(unit == null) unit = "b";
		this.actions.push("" + unit + "-" + pos + ":" + len + "\x01");
		return this;
	}
	,insert: function(pos,text,unit) {
		if(unit == null) unit = "b";
		this.actions.push("" + unit + "+" + pos + ":" + text + "\x01");
		return this;
	}
	,get_cmd: function() {
		if(this.actions.length == 0) return "";
		var tmp = this.actions.join("@");
		var cmd = "--patch " + this.fileName + "@" + tmp + "\n";
		return cmd;
	}
};
var Socket = function() {
	this.s = new js_node_net_Socket();
	this.datas = [];
	this.isConnected = false;
	this.isClosed = false;
	this.hasError = false;
};
Socket.__name__ = true;
Socket.prototype = {
	onConnect: function(callback) {
		if(callback != null) callback(this);
	}
	,onError: function(err,callback) {
		if(callback != null) callback(this,err);
	}
	,onData: function(data,callback) {
		data = data.toString();
		this.datas.push(data);
		if(callback != null) callback(this,data);
	}
	,onClose: function(callback) {
		this.isConnected = false;
		this.isClosed = true;
		if(callback != null) callback(this);
	}
	,connect: function(host,port,onConnect,onData,onError,onClose) {
		var _g = this;
		this.s.on("error",function(err) {
			_g.hasError = true;
			_g.onError(err,onError);
		});
		this.s.on("data",function(data) {
			_g.hasError = false;
			_g.onData(data,onData);
		});
		this.s.on("close",function() {
			_g.onClose(onClose);
		});
		this.s.connect(port,host,function() {
			_g.isConnected = true;
			_g.hasError = false;
			_g.onConnect(onConnect);
		});
	}
	,write: function(text) {
		return this.s.write(text);
	}
	,readAll: function() {
		return this.s.read();
	}
};
var Tool = function() { };
Tool.__name__ = true;
Tool.displayAsInfo = function(s) {
	Vscode.window.showInformationMessage(s);
};
Tool.displayAsError = function(s) {
	Vscode.window.showErrorMessage(s);
};
Tool.displayAsWarning = function(s) {
	Vscode.window.showWarningMessage(s);
};
Tool.byteLength = function(str) {
	return js_node_buffer_Buffer.byteLength(str);
};
var Vscode = require("vscode");
var js_Boot = function() { };
js_Boot.__name__ = true;
js_Boot.__string_rec = function(o,s) {
	if(o == null) return "null";
	if(s.length >= 5) return "<...>";
	var t = typeof(o);
	if(t == "function" && (o.__name__ || o.__ename__)) t = "object";
	switch(t) {
	case "object":
		if(o instanceof Array) {
			if(o.__enum__) {
				if(o.length == 2) return o[0];
				var str2 = o[0] + "(";
				s += "\t";
				var _g1 = 2;
				var _g = o.length;
				while(_g1 < _g) {
					var i1 = _g1++;
					if(i1 != 2) str2 += "," + js_Boot.__string_rec(o[i1],s); else str2 += js_Boot.__string_rec(o[i1],s);
				}
				return str2 + ")";
			}
			var l = o.length;
			var i;
			var str1 = "[";
			s += "\t";
			var _g2 = 0;
			while(_g2 < l) {
				var i2 = _g2++;
				str1 += (i2 > 0?",":"") + js_Boot.__string_rec(o[i2],s);
			}
			str1 += "]";
			return str1;
		}
		var tostr;
		try {
			tostr = o.toString;
		} catch( e ) {
			return "???";
		}
		if(tostr != null && tostr != Object.toString && typeof(tostr) == "function") {
			var s2 = o.toString();
			if(s2 != "[object Object]") return s2;
		}
		var k = null;
		var str = "{\n";
		s += "\t";
		var hasp = o.hasOwnProperty != null;
		for( var k in o ) {
		if(hasp && !o.hasOwnProperty(k)) {
			continue;
		}
		if(k == "prototype" || k == "__class__" || k == "__super__" || k == "__interfaces__" || k == "__properties__") {
			continue;
		}
		if(str.length != 2) str += ", \n";
		str += s + k + " : " + js_Boot.__string_rec(o[k],s);
		}
		s = s.substring(1);
		str += "\n" + s + "}";
		return str;
	case "function":
		return "<function>";
	case "string":
		return o;
	default:
		return String(o);
	}
};
var js_node_buffer_Buffer = require("buffer").Buffer;
var js_node_net_Socket = require("net").Socket;
var $_, $fid = 0;
function $bind(o,m) { if( m == null ) return null; if( m.__id__ == null ) m.__id__ = $fid++; var f; if( o.hx__closures__ == null ) o.hx__closures__ = {}; else f = o.hx__closures__[m.__id__]; if( f == null ) { f = function(){ return f.method.apply(f.scope, arguments); }; f.scope = o; f.method = m; o.hx__closures__[m.__id__] = f; } return f; }
String.__name__ = true;
Array.__name__ = true;
})(typeof window != "undefined" ? window : typeof exports != "undefined" ? exports : typeof self != "undefined" ? self : this);
